require 'rails_helper'

RSpec.feature "User writes a review" do
  context "As a user" do
    scenario "I must navigate to the Recipe show page to create a review" do
    end

    scenario "I must be signed in to create a review" do
    end

    scenario "I must provide a description and a rating to create a review" do
    end

    scenario "I expect an error if I don't provide a description" do
    end

    scenario "I expect an error if I don't provide a rating" do
    end
  end
end
